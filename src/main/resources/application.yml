server:
  port: 7777

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/snapp_taxi
    username: postgres
    password: 30173017


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: create-drop
    show-sql: false